
<h3><%= @users.length %>  users at <%= @school %></h3><br />
<%= @posts.length.to_s %> book posts.
<p>
<ol>
<% @users.each do |u| %>
<li><%= u.first_name %>, <%= u.email %></li>
<% end %>
</ol>
</p>

<%= link_to 'Back to admin homepage', :action => 'index' %>
